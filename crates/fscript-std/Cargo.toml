[package]
name = "fscript-std"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
fscript-ir = { path = "../fscript-ir" }
fscript-runtime = { path = "../fscript-runtime" }

[dev-dependencies]
fscript-source = { path = "../fscript-source" }
